# Audio-separation-using-ai

When you want to run the file, make sure that you had installed the required python packages like Spleeter. And create an empty directory for the Output audio to be stored.
